# 密评备考系统 - 考试优化方案

## 一、考试要求分析

### 1.1 考核内容与占比

| 考核内容 | 占比 | exam_category | 优先级 |
|----------|------|---------------|--------|
| 密码应用与安全性评估实务综合 | 30% | 密码应用与安全性评估实务综合 | 1 |
| 密码技术基础及相关标准 | 20% | 密码技术基础及相关标准 | 2 |
| 密码产品原理、应用及相关标准 | 20% | 密码产品原理、应用及相关标准 | 3 |
| 密评理论、技术及相关标准 | 20% | 密评理论、技术及相关标准 | 4 |
| 密码政策法规 | 10% | 密码政策法规 | 5 |

### 1.2 题型与分值

| 题型 | 题量 | 分值/题 | 总分 |
|------|------|---------|------|
| 单项选择题 | 60 | 0.5 | 30分 |
| 多项选择题 | 60 | 1 | 60分 |
| 判断题 | 20 | 0.5 | 10分 |
| **合计** | **140** | - | **100分** |

### 1.3 答题限时
90分钟

---

## 二、当前系统问题分析

### 2.1 数据不一致问题

| 模块 | 当前数据来源 | 问题 |
|------|--------------|------|
| 分类练习 | law_category, tech_category | 与考试要求不匹配 |
| 文档复习 | original_document | 数据独立，不与其他模块共享 |
| 智能复习 | wrong_answers | 数据独立 |
| 随机练习 | 无分类过滤 | 无针对性 |

### 2.2 优先级算法问题

**当前：** 按document_priority排序（1-5）
**问题：** 不考虑考试占比，无法突出重点

### 2.3 题型分布问题

| 题型 | 当前题库 | 考试要求 | 匹配度 |
|------|----------|----------|--------|
| 单项选择题 | 1757 | 60 | ❌ 严重超标 |
| 多项选择题 | 1757 | 60 | ❌ 严重超标 |
| 判断题 | 1604 | 20 | ❌ 严重超标 |

---

## 三、优化方案

### 3.1 统一数据源

**原则：** 所有模块使用统一的数据源和统计API

```
统一统计API: /api/v2/stats/user/{userId}
├── overall: 整体统计
├── by_exam_category: 按考试类别统计（新增）
├── by_type: 按题型统计
├── by_document: 按文档统计
├── by_law_category: 按法律法规分类
└── by_tech_category: 按技术专业分类
```

### 3.2 新增exam_category统计

在后端unified-stats-api.js中添加：

```javascript
// 按考试类别统计
const examCategoryStatsQuery = `
  SELECT
    q.exam_category,
    COUNT(*) as total_questions,
    COUNT(DISTINCT ph.question_id) as practiced_questions,
    COUNT(DISTINCT ph.question_id) FILTER (WHERE ph.is_correct = true) as correct_questions,
    ROUND(
      COUNT(DISTINCT ph.question_id) FILTER (WHERE ph.is_correct = true)::numeric /
      NULLIF(COUNT(DISTINCT ph.question_id), 0) * 100,
      1
    ) as accuracy_rate
  FROM questions q
  LEFT JOIN (
    SELECT DISTINCT question_id, is_correct
    FROM practice_history
    WHERE user_id = $1
  ) ph ON q.id = ph.question_id
  WHERE q.exam_category IS NOT NULL
  GROUP BY q.exam_category
  ORDER BY
    CASE q.exam_category
      WHEN '密码应用与安全性评估实务综合' THEN 1
      WHEN '密码技术基础及相关标准' THEN 2
      WHEN '密码产品原理、应用及相关标准' THEN 3
      WHEN '密评理论、技术及相关标准' THEN 4
      WHEN '密码政策法规' THEN 5
    END
`;
```

### 3.3 优先级算法调整

**新算法：** 综合考虑考试占比、正确率、练习进度

```javascript
// 计算每个类别的优先级分数
function calculatePriorityScore(category) {
  const examWeight = getExamWeight(category.exam_category); // 考试占比权重
  const accuracy = category.accuracy_rate || 0;
  const completionRate = category.practiced_questions / category.total_questions;

  // 优先级分数 = 考试权重 × (1 - 完成度) × (1 + (1 - 正确率))
  // 考试占比越高、完成度越低、正确率越低，优先级越高
  return examWeight * (1 - completionRate) * (1 + (1 - accuracy / 100));
}

// 考试权重
function getExamWeight(examCategory) {
  const weights = {
    '密码应用与安全性评估实务综合': 0.30,
    '密码技术基础及相关标准': 0.20,
    '密码产品原理、应用及相关标准': 0.20,
    '密评理论、技术及相关标准': 0.20,
    '密码政策法规': 0.10
  };
  return weights[examCategory] || 0.10;
}
```

### 3.4 前端组件调整

#### 3.4.1 添加考试类别练习组件

新建 `ExamCategoryPractice.vue`：
- 显示5个考试类别
- 按考试占比和优先级分数排序
- 显示每个类别的统计数据

#### 3.4.2 调整首页显示

在首页添加"按考试类别练习"入口，放在最前面

#### 3.4.3 文档复习组件调整

在DocumentReview.vue中：
- 添加按exam_category筛选
- 按优先级分数排序文档

#### 3.4.4 智能复习组件调整

在SmartReview.vue中：
- 优先推荐优先级分数高的类别的错题
- 考虑考试占比权重

### 3.5 题型分布优化

虽然无法改变题库总数，但可以：
1. 在练习时优先推荐考试重点题型
2. 多选题分值最高，应作为练习重点
3. 判断题相对简单，可以快速刷题

---

## 四、实施步骤

### 第一步：后端API扩展（已存在exam_category字段）
1. ✅ 确认questions表有exam_category字段
2. ⏳ 在unified-stats-api.js中添加exam_category统计
3. ⏳ 测试API返回数据

### 第二步：前端组件开发
1. ⏳ 创建ExamCategoryPractice.vue组件
2. ⏳ 修改首页，添加考试类别入口
3. ⏳ 调整DocumentReview.vue排序算法
4. ⏳ 调整SmartReview.vue推荐算法

### 第三步：数据同步
1. ⏳ 确保所有组件使用统一API
2. ⏳ 实时更新练习进度
3. ⏳ 同步错题数据

### 第四步：测试验证
1. ⏳ 测试各模块数据一致性
2. ⏳ 验证优先级算法效果
3. ⏳ 确认用户体验提升

---

## 五、预期效果

### 5.1 学习效率提升
- 按考试占比学习，重点明确
- 优先学习未掌握的重点内容
- 避免在次要内容上浪费时间

### 5.2 数据一致性
- 所有模块显示的统计数据一致
- 练习进度实时同步
- 错题数据统一管理

### 5.3 用户体验
- 清晰的学习路径
- 直观的进度展示
- 智能的学习推荐

---

## 六、需要确认的问题

1. **exam_category字段数据完整性**
   - 当前数据库中所有题目都有exam_category吗？
   - 是否需要补充缺失的数据？

2. **题型分布调整**
   - 是否需要在题库中按考试比例筛选题目？
   - 还是保持当前题库，只调整推荐算法？

3. **现有功能保留**
   - 保留law_category和tech_category分类吗？
   - 还是完全替换为exam_category？

4. **优先级算法细化**
   - 是否需要考虑更多因素（如难度、遗忘曲线）？
   - 间隔重复算法如何与考试优先级结合？
